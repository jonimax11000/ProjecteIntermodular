<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Agregar enlace en el menú principal del portal -->
    <template id="portal_my_home_menu_subscription" name="Portal layout: subscription menu entries" inherit_id="portal.portal_breadcrumbs" priority="40">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'subscription'" t-attf-class="breadcrumb-item active">
                <a href="/my/subscriptions">Mis Suscripciones</a>
            </li>
        </xpath>
    </template>

    <!-- Agregar tarjeta en "Mi Cuenta" -->
    <template id="portal_my_home_subscription" name="Portal My Home: subscription entries" inherit_id="portal.portal_my_home" priority="40">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-call="portal.portal_docs_entry">
                <t t-set="title">Suscripciones</t>
                <t t-set="url" t-value="'/my/subscriptions'"/>
                <t t-set="placeholder_count" t-value="'subscription_count'"/>
            </t>
        </xpath>
    </template>
</odoo>